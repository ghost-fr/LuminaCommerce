<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        x:Class="Lumina.Pos.UI.MainWindow"
        Title="Lumina POS"
        Width="900" Height="650">
    <!--
      PLACEHOLDER — this file exists only so the app has an entry point and the
      solution builds. Grok: replace this entire window's content with the real
      login screen + capability-gated nav shell per docs/CONTRACTS.md §0. The
      backend is fully wired and resolvable via App.Services (IAuthService,
      IPosSaleService, IProductCatalogueService, etc.) — see App.axaml.cs.
    -->
    <StackPanel Margin="24" Spacing="12">
        <TextBlock FontSize="20" FontWeight="Bold" Text="Lumina POS — backend wired, UI pending" />
        <TextBlock TextWrapping="Wrap"
                   Text="Replace MainWindow.axaml with the login screen and capability-gated nav shell. App.Services exposes the full backend DI container." />
    </StackPanel>
</Window>
