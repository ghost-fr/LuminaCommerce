# EF Core persistence (placeholder)

Phase 1 work item. Will contain `LuminaDbContext`, entity configurations, and migrations.
SQLite for local/dev; provider swap to PostgreSQL/SQL Server is config-only per the
blueprint's infrastructure layer.
